import { CsswitheventDirective } from './csswithevent.directive';

describe('CsswitheventDirective', () => {
  it('should create an instance', () => {
    const directive = new CsswitheventDirective();
    expect(directive).toBeTruthy();
  });
});
